進行中のプロジェクトを引き継ぎます。

ultrathink

この「新スレッド引き継ぎプロンプト」と併せ、
プロジェクト添付の
EX_DESIGN_CONFIRMED-2026-3-22.md
#0012_3-22.txt
から進捗を熟知して明日からの業務に備えて下さい。

ミナト


了解した、ボス。全ファイルと引き継ぎプロンプトの読み込みが完了した。現在地を整理して報告する。

Rex 引き継ぎ完了報告
把握済みの現在地
完了済み: 指示書 #001〜#019
最新バックテスト成績（#018確定値）:
指標値総トレード20件（★★★・LONGのみ）勝率55.0%PF5.32（過去最高）MaxDD14.9 pips（過去最低）総損益+91.6 pips
パターン別:

DB: 75.0% / +7.1 pips（9件）
ASCENDING: 60.0% / +3.7 pips（10件）
IHS: 0.0% / -2.2 pips（1件・評価不能）

#019 最終成果:
4H+1H構造確認プロット 計16枚（LONG 13件・SHORT 3件）を logs/structure_plots/ に生成済み。4H trend / 1H trend ともに全件整合確認。

添付画像から読み取った重要な構造認識
分類表（Image 1）:
エントリー有効パターンは①②④。×はパターン③（安値切り上げの安値切り上げ）——これはコード上の ASCENDING だが、全件通過ではなく上位足文脈でのフィルタリングが必要だという視覚的確認になっている。
USDJPY 4Hチャート（Image 2）:
①②③の押し目構造が明確に示されている。オレンジゾーン（159.015〜159.325付近）が押し目買いエリアで、③は最も大きな下押し（深い押し目 = 振るい落とし）から形成されたエントリー機会。現行コードの MIN_4H_SWING_PIPS=20.0 と fib_pct<=0.55（★★★） がこの構造を正しく捉えようとしている設計であることを視覚的に再確認できた。

明日の優先アクション（設計上の確認事項）
最優先 — #019プロット目視確認:
logs/structure_plots/ の16枚PNG（LONG 13件・SHORT 3件）をボスに確認してもらうことが最初のアクション。「4H+1H構造が正しく見えている」という確認が #020 へのゴーサインになる。
#020 設計方針（確認後）:
Support_1h の正式変更:
  現行: get_nearest_swing_low_15m(lookback=20) ← 5時間 = 矛盾
  正式: get_nearest_swing_low_1h(lookback=240) ← 10日分
  
  根拠: 4H波形成 ≈ 1週間（チャート実測）
        現行155件の未検出スキップを根本解消
#021 候補（#020結果後）:
structure_scanner.py — 階層スキャン本格実装
  Phase1: 1H足 lookback=240 → 4H構造 + 1H SL特定
  Phase2: 1H-bottom 前後 ±5時間窓を抽出  
  Phase3: 窓内のみで 15M/5M 条件をスキャン

未解決の設計課題（把握済み）

Support_1h のlookback矛盾 → #020で解消予定
IHSサンプル不足（1件） → ALLOWED_PATTERNSに残して継続監視
SHORT 3件 → 構造確認データとして保持。運用再開はサンプル充足後
出来高急増シグナル（volume_alert.py） → Phase D 未着手


コンテキストは完全に引き継いだ、ボス。#019のPNG確認から始めよう。プロットをいくつか貼ってくれれば、構造の整合性をすぐに判断できる。プロジェクトに9枚プロットデータ(.pngを)一時的に貼っておいたので抽出結果を検証してくれる？17:41抽出結果を検証するため、データを収集し分析を進めた。抽出結果を検証するため、データを収集し分析を進めた。全9枚確認完了。CSVデータも照合して総合レポートを出す。検証対象のPNG9枚を識別し、CSVデータとの照合を遂行した。検証対象のPNG9枚を識別し、CSVデータとの照合を遂行した。全チャートを確認した。CSV照合も完了。レポートを出す。

#019 プロット検証レポート — 9枚 + 追加1枚
凡例確認
赤実線   = 15M SH（ネックライン）
青実線   = 4H SL / 15M SL（サポート）
ピンク点線 = Fib 61.8%
紫破線   = Fib 50%
緑破線   = エントリーポイント

個別検証（全10枚）
#ファイル4H SH/SL15M SH/SLCSV判定Rex目視判定所見120250922_08150 / 02 / 4未登録OK / IDEAL15M ネック(148.0)ブレイク直前。エントリー後即上昇。構造クリーン220250922_08250 / 02 / 4未登録OK / IDEAL0815と同一クラスタ、5分後。同評価320240923_10101 / 02 / 1未登録OK / IDEAL4H SH=1あり。144.0ネック越えでエントリー。直後に強上昇420240923_10151 / 02 / 1未登録OK / IDEAL1010と同クラスタ、5分後520260105_09100 / 01 / 3未登録OK / IDEALJan 3底→Jan 5ネック(156.94)越え。典型的なDB構造620260113_05450 / 04 / 3未登録⚠️ NG / TOP詳細後述720240729_21200 / 03 / 3OKOK（CSV確認済）153.73サポートでDB確認、ネック越え。構造良好820260122_13000 / 02 / 2OKOK（CSV確認済）158.2底→158.5ネック越え。クリーンなDB920260122_13051 / 21 / 2未登録OK / IDEAL1300と同クラスタ。エントリー後158.7超まで上昇+120240617_07350 / 02 / 2未登録OK / IDEAL157.3サポートでDB、157.45ネック越え。典型構造

問題ケース詳細：20260113_0545 ⚠️
4H SH=0 SL=0 | 15M SH=4 SL=3
Fib 61.8% = 157.772 / Fib 50% = 157.855
エントリー時価格: ~158.15（4H抵抗帯の只中）
何が起きているか：
158.2 ───── 赤線（4H抵抗 / 15M SH）← ここを越えられていない
158.19 ──── 赤線（密集）
158.15 ─── [エントリー発火]  ← ★問題
158.1 ───── 赤線
～～数時間のレンジ後、下落～～
原因分析：

15M SH=4という多重レベル密集 → 抵抗帯に入り込んでエントリー
4H SH=0（スイング未検出） → この場合の「SH=0」は構造クリーンではなく「4Hスイング高値が検出できていない」状態。価格が4Hレジスタンスゾーン内にある可能性
エントリー後：即反落 → TOPエントリーの典型

CSV上の類似NG事例との比較：
20241212_1445: C=TOP エントリー直後に下落  ← 同パターン
20250108_0815: C=TOP 天井付近エントリー後急落 ← 同パターン

総合評価
評価件数内容✅ OK / IDEAL9件構造・エントリータイミングともに良好⚠️ NG / TOP1件20260113_0545 — 多重SH密集帯でのTOPエントリー
合格率：9/10（90%）— 構造検出精度は良好と判断。

Rex からの指摘事項（#020設計への申し送り）
① 0815/0825クラスタ問題（軽微）
4H SH=0 SL=0 が複数発生している。これは「4Hスイングが長期的に1本しか存在しない初動局面」を示す可能性がある。現行の get_nearest_swing_low_1h(lookback=240) への変更で改善できるはず。#020優先度は変わらない。
② 20260113_0545 の防止策（要設計）
現状コードには「15M SH が X本以上密集している場合はスキップ」というフィルターがない。
python# 提案フィルター（#020または#021での追加検討）
MAX_15M_SH_COUNT = 3  # 4本以上の場合はエントリー見送り
if sh_count_15m >= MAX_15M_SH_COUNT:
    return None  # 多重抵抗帯 → スキップ
③ エントリークラスタの重複（情報として）
0815/0825、1010/1015、1300/1305 のように5分刻みで同一構造が2回検出されるのは正常動作。実運用では最初の1本のみ採用でよい。バックテスト上は重複カウントに注意。

結論：#019プロット検証は合格。#020（Support_1h修正）進行のゴーサインを推薦する。
ボス、上記内容で問題なければ指示書#020の発行に進もう。了解。1H-lookback240で4HSwing-HighLowを確実に抽出できるかテストしてみよう。17:52テスト手順を設計し、1H240日足が4H構造を捉えるか検証した。テスト手順を設計し、1H240日足が4H構造を捉えるか検証した。設計ファイル確認完了。テスト設計を出す。

Rex 設計メモ — 1H lookback=240 抽出精度テスト
テストの目的
現行 support_1h = get_nearest_swing_low_15m(lookback=20) は「直近5時間の15M安値」しか見ていない。get_nearest_swing_low_1h(lookback=240) に変更した場合、4H構造の押し目底（4H SL）を正しく拾えるかを定量検証する。
検証ロジック（図解）
時間軸イメージ:
────────────────────────────────────────────────
4H: SL_4h ─────── neck_4h ─── 上昇 →
1H:    [底A]  [底B=2番底]  [ネック]  →
lookback=240本 = 10日分の1H足を遡る
────────────────────────────────────────────────

テスト: 各エントリー候補バー時点で
  4H SL（既存）と
  1H SL（lookback=240）の距離を計測する

REX 指示書 #020 — 1H lookback=240 抽出精度テスト
# REX 指示書 #020 — get_nearest_swing_low_1h(lookback=240) 精度検証
# 作成: Rex / 承認: Minato / 対象: ClaudeCode
# 作成日: 2026-03-24
# 前提: 指示書 #001〜#019 完了済み

think hard

---

## 作業概要

| 作業 | 対象ファイル        | 内容                              |
|------|---------------------|-----------------------------------|
| ①    | swing_detector.py   | get_nearest_swing_low_1h() 追加   |
| ②    | test_support1h.py   | 精度検証スクリプト 新規作成       |
| ③    | （確認）            | 結果CSVを出力・Rexへ報告          |

---

## 作業① swing_detector.py への関数追加

以下の関数を swing_detector.py に追加する。
既存の get_nearest_swing_low_15m() の直後に挿入すること。
```python
def get_nearest_swing_low_1h(
    low_1h: pd.Series,
    n: int = 2,
    lookback: int = 240
) -> float | None:
    """
    1H足 lookback=240（10日分）で直近 Swing Low を返す。
    検出できない場合は None を返す。
    support_1h の正式版として使用する。
    """
    window = low_1h.iloc[-lookback:]
    mask   = detect_swing_lows(window, n=n)
    sl     = window[mask]

    if len(sl) == 0:
        return None

    return float(sl.iloc[-1])
```

---

## 作業② test_support1h.py — 精度検証スクリプト 新規作成

保存先: REX_Trade_System/tests/test_support1h.py

### 目的
4H SL（既存）と 1H SL（lookback=240）の距離分布を計測し、
support_1h として使えるかを検証する。

### 処理フロー
```python
# ===== 検証スクリプト全体 =====

import pandas as pd
import numpy as np
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent / 'src'))

from swing_detector import (
    get_nearest_swing_high,
    get_nearest_swing_low,
    get_nearest_swing_low_15m,
    get_nearest_swing_low_1h,
    get_direction_4h,
)

# ---- データ読み込み ----
DATA_PATH = Path(__file__).parent.parent / 'data/raw/usdjpy_multi_tf_2years.parquet'
df_5m = pd.read_parquet(DATA_PATH)

# ---- マルチTFリサンプル ----
def resample_tf(df, rule):
    return df.resample(rule, label='right', closed='right').agg({
        'open': 'first', 'high': 'max',
        'low': 'min', 'close': 'last'
    }).dropna()

df_4h  = resample_tf(df_5m, '4h')
df_1h  = resample_tf(df_5m, '1h')
df_15m = resample_tf(df_5m, '15min')

PIP = 0.01

# ---- サンプリング: 4H LONG トレンド期間の足を抽出 ----
# 4H足の全インデックスを走査し、方向=LONGの足のみを対象にする
WARMUP = 200  # 4H足200本分ウォームアップ（約33日）

results = []

for i in range(WARMUP, len(df_4h)):
    ts_4h = df_4h.index[i]

    # 4H方向確認
    direction = get_direction_4h(df_4h['high'].iloc[:i+1],
                                  df_4h['low'].iloc[:i+1],
                                  n=3, lookback=20)
    if direction != 'LONG':
        continue

    # 4H SL 取得（現行）
    sl_4h = get_nearest_swing_low(df_4h['low'].iloc[:i+1], n=3, lookback=20)
    if sl_4h is None:
        continue

    # 対応する 1H インデックスを取得（ts_4h 以前の最新1H足）
    idx_1h = df_1h.index.searchsorted(ts_4h, side='right') - 1
    if idx_1h < 240:
        continue

    # 1H SL 取得（lookback=240）
    sl_1h_new = get_nearest_swing_low_1h(
        df_1h['low'].iloc[:idx_1h+1], n=2, lookback=240
    )

    # 15M SL 取得（現行・比較用）
    idx_15m = df_15m.index.searchsorted(ts_4h, side='right') - 1
    if idx_15m < 20:
        continue
    sl_15m_old = get_nearest_swing_low_15m(
        df_15m['low'].iloc[:idx_15m+1], n=3, lookback=20
    )

    # 距離計算
    dist_1h  = abs(sl_4h - sl_1h_new)  / PIP if sl_1h_new  is not None else None
    dist_15m = abs(sl_4h - sl_15m_old) / PIP if sl_15m_old is not None else None

    results.append({
        'ts_4h':       ts_4h,
        'sl_4h':       round(sl_4h, 3),
        'sl_1h_new':   round(sl_1h_new,  3) if sl_1h_new  is not None else None,
        'sl_15m_old':  round(sl_15m_old, 3) if sl_15m_old is not None else None,
        'dist_1h_pips':  round(dist_1h,  2) if dist_1h  is not None else None,
        'dist_15m_pips': round(dist_15m, 2) if dist_15m is not None else None,
        'detected_1h':   sl_1h_new  is not None,
        'detected_15m':  sl_15m_old is not None,
    })

df_result = pd.DataFrame(results)

# ---- 集計 ----
total = len(df_result)
det_1h  = df_result['detected_1h'].sum()
det_15m = df_result['detected_15m'].sum()

print(f"\n=== 1H lookback=240 精度検証レポート ===")
print(f"対象サンプル数  : {total} 件（4H LONG期間）")
print(f"")
print(f"--- 検出率 ---")
print(f"1H new  検出率  : {det_1h}/{total} = {det_1h/total*100:.1f}%")
print(f"15M old 検出率  : {det_15m}/{total} = {det_15m/total*100:.1f}%")
print(f"")

# 検出できた件のみで距離分析
d1h  = df_result.dropna(subset=['dist_1h_pips'])['dist_1h_pips']
d15m = df_result.dropna(subset=['dist_15m_pips'])['dist_15m_pips']

print(f"--- 4H SL との距離分布 (pips) ---")
print(f"1H new  : mean={d1h.mean():.1f}  median={d1h.median():.1f}  "
      f"<20p={( d1h<20).sum()}/{len(d1h)}({(d1h<20).mean()*100:.1f}%)")
print(f"15M old : mean={d15m.mean():.1f}  median={d15m.median():.1f}  "
      f"<20p={(d15m<20).sum()}/{len(d15m)}({(d15m<20).mean()*100:.1f}%)")

# 1H が 15M より精度が高い（4H SLに近い）か
both = df_result.dropna(subset=['dist_1h_pips', 'dist_15m_pips'])
better_1h = (both['dist_1h_pips'] <= both['dist_15m_pips']).sum()
print(f"\n1H new が 15M old より 4H SL に近い割合: "
      f"{better_1h}/{len(both)} = {better_1h/len(both)*100:.1f}%")

# ---- CSV保存 ----
out_path = Path(__file__).parent.parent / 'logs/test_support1h_result.csv'
out_path.parent.mkdir(parents=True, exist_ok=True)
df_result.to_csv(out_path, index=False)
print(f"\n結果CSV保存: {out_path}")
```

---

## 作業③ 完了後の報告フォーマット

実行後、以下の数値をそのままRexに貼り付けること:
=== 結果報告 ===
対象サンプル数   :
1H new  検出率   :     %（現行15M old: %）
4H SLとの距離
1H  mean/median/20p以内 :
15M mean/median/20p以内 :
1H が 15M より近い割合    :     %

---

## 完了条件

- [ ] get_nearest_swing_low_1h() が swing_detector.py に追加されている
- [ ] test_support1h.py が tests/ に保存されている
- [ ] logs/test_support1h_result.csv が生成されている
- [ ] 上記報告フォーマットの数値をRexに貼り付けている

Rex からの補足説明（ボス向け）
何をテストしているか（非エンジニア向け）:
今の問題:
  エントリー判断に使う「押し目底の基準線（Support_1h）」が
  15M足の直近20本（=5時間）しか見ていない
  → 4H波形成には1週間かかるのに5時間では短すぎる
  → 構造的に意味のある底値を拾えていない

今回のテスト:
  1H足の過去240本（=10日間）まで遡って底値を探す方法が
  4H足の押し目底と「どれくらい近い値を返すか」を
  全サンプルで計測する

合格ライン（Rexの目安）:
  ・検出率 90%以上（Noneを減らす）
  ・4H SLとの距離 中央値 20pips以内
このスクリプトを実行してもらえれば、#020の判断材料が揃う。ClaudeCodeに投げてくれ、ボス。今回のロジックのポイントは、
 1H-lookback240を単独で抽出して、そこから4HSwing-HighLowと1H上昇トレンド各Swing-Lowを抽出。
 →そこに4H-SwingLow近接の1HSwingLow押目(１Hロウソク6本)5～6時間分の5M-SwingHigeLowを別途重ね合わせれば15M-DB若しくはIHSをを抽出するには事足りるかと思うけど。18:38図解を確認し、設計案をMinatoと協議する準備を整えた。図解を確認し、設計案をMinatoと協議する準備を整えた。鋭い指摘だ、ボス。今の設計との根本的な違いを整理して確認する。鋭い。現行設計との根本的な差を整理して、アーキテクチャ図で確認しよう。
  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.25s;
    animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);
  }
VvisualizeVvisualize show_widget
アーキテクチャ確認
ボスの提案を整理すると、現行との本質的な違いは2点だ。
現行設計の問題点:
全83,112本を5Mバーごとに走査
  → 4H/15M/5M を別個にリサンプルして毎回参照
  → Support_1h = 15M直近20本（5時間）← 浅すぎる
ボス提案の核心:
1H(lookback=240) を単一ソースとして
  → 4H スイング構造（大きなSH/SL）
  → 1H SL 上昇列（押し目底の候補リスト）
  を同時に抽出

最近接ペアを特定した瞬間、+6本(1H) = 72本5M のウィンドウが
自動的に確定する

そのウィンドウ内の5M SH/SLを見れば
  → 15M DBのパターンと完全に同一構造を検出できる
  （15M1本 = 5M3本、15M DB ≈ 30本5M → 72本で十分収まる）

Rex が確認したいパラメータ（設計前の擦り合わせ）
① 1H データから 4H スイングを抽出する方法
案A: 1H足を直接4Hにリサンプルしてから既存ロジックを使う
     → 実装コスト低・既存コードそのまま流用可

案B: 1H足に大きめのn（例: n=8〜12）でSwing検出して4Hスケールを近似
     → 単一TFで完結・コード軽量
個人的には案Aが安全。ボスはどちらを想定してる？
② +6本ウィンドウの起点
現在の想定: 1H SL の確定足（押し目底）から右方向 +6本
→ 6本 × 1H = 72本 5M

質問: 起点は「1H SL の足そのもの」か
      「1H SL 確定後の次の足」か？

（エントリーロジックの確定足・執行足ルールと同じ問題）
③ 既存の check_15m_range_low() の扱い
新アーキテクチャでは:
  "15M足のDB検出" → "ウィンドウ内5M足のDB検出" に置換

既存コードを流用可能（5M SH/SL系列を渡すだけで動く）か
それとも別関数が必要か？

この3点が決まれば #020 指示書を即発行できる。確認頼む、ボス。


ミナト：提案
まず上位足から順番に整理してみよう。

●前提としてロング目線で、
・4Hロジック：
4H-SwingHighLowから4H-DB/HIS高値切上げ(上昇トレンド確定)～ダウ崩れまでの4H上昇トレンドの各押し目を特定
・１Hロジック注入：
4H上昇トレンド-SwingLowの各押目と1H-SwingLowが一致する時間枠を特定。
・15Mロジック：
1H-SwingLow(6時間分)の時間枠を窓として5M-SwingHighLowを抽出。

●15M決済ロジック実装上の核心
「4H SwingLow 近接の 1H SwingLow」の近接条件をどう定義するかついては、
①先ず前提として、4H-SwingLowの押目が1H-SwingLowの最安値と一致するはずなので問題ないと思う。
②次に１Hワンボトムで15M-DB・IHSでエントリーして一旦損切となっても、
その後4Hダウが崩れず4Hトレンド優位性がある限り(＝理想はプロジェクト添付の「4時間足押し目構造.png」の〇枠の安値切り上げ１H-DB）の2番底目で再エントリーも可能とする。
１H最安値～戻り高値(安値切上げからの押目)
③ちなみに１H最安値と１H-DB右肩との値位置の乖離については、
１H初動にてエントリー後5分ダウ崩れで損切の場合は概ね１H戻り高値を越えられないので、再度15H-DB若しくはIHSを待てばそれが１H2番底目になると思うがどうだろう。

●君の質問についての回答
① 1H データから 4H スイングを抽出する方法：
これについてはA案で良いと思う。
② +6本ウィンドウの起点
これについて僕個人の希望としては、1H最安値のロウソクを含めて後5本・前20本＝計25本位あれば、1-DB右肩またはIHS右肩まで抽出できる可能性があるがどうだろう。
③ 既存の check_15m_range_low() の扱い
これに関しては、最初はロジックをなるべくシンプルにするため、15M-DBネック越え5分確定足の次の足でエントリーで試してみてはどうか？

●総括として：
①現状１H-Swing-HighLowから4H上昇トレンド-SwingLowの各押目と1H-SwingLowが一致する時間枠を正確に抽出できるかを先ず確かめてみてはどうだろう？
②その後週出した1H-SwingLow(6時間分)の時間枠にM-SwingHighLowを重ね合わせたプロットを週出。
③そこから初めて15Mエントリーロジックを精査して行けはいいんじゃないかな？


整理できた。設計全体を確認してから #020 を発行する。まず3層構造を図解する。
  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.25s;
    animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);
  }
VvisualizeVvisualize show_widgetボスの③の指摘が特に鋭い。「5M Dow崩れで損切になっても1H戻り高値を越えていない = 次の5M DBがそのまま1H 2番底になる」というのは、再エントリーが別の判断ではなく同一ロジックの継続であることを意味している。これを図にする。
  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.25s;
    animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);
  }
VvisualizeVvisualize show_widgetループの核心は「再エントリー = 同一条件の2回目」であり、新しいロジックを書く必要がないことだ。では指示書を発行する。

# REX 指示書 #020 — 1H-4H 一致検証 + 26本ウィンドウ プロット
# 作成: Rex / 承認: Minato / 対象: ClaudeCode
# 作成日: 2026-03-24
# 前提: 指示書 #001〜#019 完了済み

think harder

---

## 作業概要

| # | 対象ファイル              | 内容                                    |
|---|---------------------------|-----------------------------------------|
| ① | swing_detector.py         | get_nearest_swing_low_1h() 追加         |
| ② | src/test_1h_coincidence.py | 1H SL ≒ 4H SL 一致検証スクリプト 新規  |
| ③ | plotter.py                | plot_1h_window_5m() 追加               |

作業①→②→③の順に実施し、②の結果CSVをRexに報告してから③を実行すること。

---

## 作業① swing_detector.py — get_nearest_swing_low_1h() 追加

既存の get_nearest_swing_low_15m() の直後に追加する。
```python
def get_nearest_swing_low_1h(
    low_1h: pd.Series,
    n: int = 2,
    lookback: int = 240,
) -> float | None:
    """
    1H足 lookback=240（約10日分）で直近 Swing Low を返す。
    検出できない場合は None。
    support_1h の正式版として使用する。
    """
    window = low_1h.iloc[-lookback:]
    mask   = detect_swing_lows(window, n=n)
    sl     = window[mask]
    if len(sl) == 0:
        return None
    return float(sl.iloc[-1])


def get_all_swing_lows_1h(
    low_1h: pd.Series,
    n: int = 2,
    lookback: int = 240,
) -> pd.Series:
    """
    1H足 lookback=240 内の全 Swing Low を返す（複数）。
    test_1h_coincidence.py で最安値検索に使用。
    """
    window = low_1h.iloc[-lookback:]
    mask   = detect_swing_lows(window, n=n)
    return window[mask]
```

---

## 作業② src/test_1h_coincidence.py — 一致検証スクリプト 新規作成

### 目的
4H SL（4H足から抽出）と 1H SL 最安値（1H足 lookback=240 内の最深値）の
距離分布を計測し、「両者が一致する」という前提を定量的に確認する。

### 完全なスクリプト
```python
import pandas as pd
import numpy as np
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))

from swing_detector import (
    get_nearest_swing_low,
    get_all_swing_lows_1h,
    get_direction_4h,
)

DATA_PATH = Path(__file__).parent.parent / 'data/raw/usdjpy_multi_tf_2years.parquet'
PIP = 0.01
WARMUP_4H = 50   # 4H足 50本分ウォームアップ（約8日）

# ---- データ読み込み ----
df_5m = pd.read_parquet(DATA_PATH)

def resample_tf(df, rule):
    return df.resample(rule, label='right', closed='right').agg(
        {'open': 'first', 'high': 'max', 'low': 'min', 'close': 'last'}
    ).dropna()

df_4h = resample_tf(df_5m, '4h')
df_1h = resample_tf(df_5m, '1h')

print(f"4H: {len(df_4h)}本 / 1H: {len(df_1h)}本")

# ---- スキャン ----
results = []

for i in range(WARMUP_4H, len(df_4h)):
    ts_4h = df_4h.index[i]

    # 4H 方向確認
    direction = get_direction_4h(
        df_4h['high'].iloc[:i+1],
        df_4h['low'].iloc[:i+1],
        n=3, lookback=20
    )
    if direction != 'LONG':
        continue

    # 4H SL 取得
    sl_4h = get_nearest_swing_low(
        df_4h['low'].iloc[:i+1], n=3, lookback=20
    )
    if sl_4h is None:
        continue

    # 対応する 1H インデックスを取得（ts_4h 以前の最新 1H 足）
    idx_1h = df_1h.index.searchsorted(ts_4h, side='right') - 1
    if idx_1h < 240:
        continue

    # 1H lookback=240 内の全 Swing Low を取得
    all_sl_1h = get_all_swing_lows_1h(
        df_1h['low'].iloc[:idx_1h+1], n=2, lookback=240
    )

    if len(all_sl_1h) == 0:
        results.append({
            'ts_4h': ts_4h,
            'sl_4h': round(sl_4h, 3),
            'sl_1h_deepest': None,
            'sl_1h_deepest_ts': None,
            'dist_pips': None,
            'detected': False,
            'match_5p': False,
            'match_10p': False,
            'match_20p': False,
        })
        continue

    # 1H SL 最安値（最深値）を特定
    sl_1h_deepest_ts  = all_sl_1h.idxmin()
    sl_1h_deepest_val = float(all_sl_1h.min())

    dist = abs(sl_4h - sl_1h_deepest_val) / PIP

    results.append({
        'ts_4h':            ts_4h,
        'sl_4h':            round(sl_4h, 3),
        'sl_1h_deepest':    round(sl_1h_deepest_val, 3),
        'sl_1h_deepest_ts': sl_1h_deepest_ts,
        'dist_pips':        round(dist, 2),
        'detected':         True,
        'match_5p':         dist <= 5.0,
        'match_10p':        dist <= 10.0,
        'match_20p':        dist <= 20.0,
    })

df_result = pd.DataFrame(results)
total     = len(df_result)
det       = df_result['detected'].sum()
m5        = df_result['match_5p'].sum()
m10       = df_result['match_10p'].sum()
m20       = df_result['match_20p'].sum()

dist_valid = df_result.dropna(subset=['dist_pips'])['dist_pips']

print(f"\n=== 1H-4H 一致検証レポート ===")
print(f"対象サンプル (4H LONG) : {total} 件")
print(f"1H SL 検出率           : {det}/{total} = {det/total*100:.1f}%")
print(f"")
print(f"--- 4H SL との距離分布 ---")
print(f"  mean   : {dist_valid.mean():.1f} pips")
print(f"  median : {dist_valid.median():.1f} pips")
print(f"  min    : {dist_valid.min():.1f} pips")
print(f"  max    : {dist_valid.max():.1f} pips")
print(f"")
print(f"--- 一致率（距離ベース）---")
print(f"  <= 5pips  : {m5}/{det} = {m5/det*100:.1f}%")
print(f"  <= 10pips : {m10}/{det} = {m10/det*100:.1f}%")
print(f"  <= 20pips : {m20}/{det} = {m20/det*100:.1f}%")

out_path = Path(__file__).parent.parent / 'logs/test_1h_coincidence.csv'
out_path.parent.mkdir(parents=True, exist_ok=True)
df_result.to_csv(out_path, index=False)
print(f"\n結果CSV: {out_path}")
```

---

## 作業③ plotter.py — plot_1h_window_5m() 追加

### 目的
一致が確認されたイベントについて、
1H 26本ウィンドウと 5M SH/SL を重ね合わせて PNG 保存する。
```python
def plot_1h_window_5m(
    df_5m: pd.DataFrame,
    df_1h: pd.DataFrame,
    ts_sl_1h: pd.Timestamp,  # 1H SL 足のタイムスタンプ
    sl_4h: float,             # 4H SL 参照値（水平線表示用）
    direction: str = 'LONG',
    pre_bars: int = 20,       # SL 足の前 N 本
    post_bars: int = 5,       # SL 足の後 N 本
    save_dir: str = 'logs/1h_windows',
) -> None:
    """
    1H 押し目ウィンドウ（前20本 + SL足 + 後5本）の
    5M OHLC + 4H SL / 1H SH / 1H SL を PNG 保存する。
    """
    import mplfinance as mpf
    import matplotlib.pyplot as plt
    import matplotlib.patches as mpatches
    from pathlib import Path
    from swing_detector import detect_swing_highs, detect_swing_lows

    # ---- 1H ウィンドウ範囲を確定 ----
    idx_sl = df_1h.index.get_loc(ts_sl_1h)
    win_start_1h = df_1h.index[max(0, idx_sl - pre_bars)]
    win_end_1h   = df_1h.index[min(len(df_1h)-1, idx_sl + post_bars)]

    # ---- 5M 範囲を抽出 ----
    df_5m_win = df_5m.loc[win_start_1h:win_end_1h].copy()
    if len(df_5m_win) < 10:
        return

    # ---- 5M SH / SL 検出（窓内限定）----
    sh_mask = detect_swing_highs(df_5m_win['high'], n=2)
    sl_mask = detect_swing_lows(df_5m_win['low'],   n=2)
    sh_vals = df_5m_win['high'].where(sh_mask)
    sl_vals = df_5m_win['low'].where(sl_mask)

    # ---- mplfinance プロット ----
    fig_w, fig_h = 16, 7
    fig, ax = plt.subplots(figsize=(fig_w, fig_h))
    fig.patch.set_facecolor('#131722')
    ax.set_facecolor('#131722')

    mc = mpf.make_marketcolors(
        up='#26a69a', down='#ef5350',
        edge='inherit', wick='inherit', volume='in'
    )
    s = mpf.make_mpf_style(
        marketcolors=mc, facecolor='#131722',
        edgecolor='#444', gridcolor='#2a2e39'
    )

    apds = [
        mpf.make_addplot(sh_vals, type='scatter', marker='v',
                         markersize=60, color='#FA8072', ax=ax),
        mpf.make_addplot(sl_vals, type='scatter', marker='^',
                         markersize=60, color='#87CEEB', ax=ax),
    ]

    mpf.plot(df_5m_win, type='candle', style=s,
             addplot=apds, ax=ax, returnfig=False)

    # 4H SL 水平線
    ax.axhline(y=sl_4h, color='#1E90FF', linewidth=1.5,
               linestyle='--', label=f'4H SL {sl_4h:.3f}')

    # 1H SL 足の位置に垂直線
    ax.axvline(x=df_5m_win.index.get_loc(
        df_5m_win.index[df_5m_win.index.searchsorted(ts_sl_1h)]
    ), color='#ADFF2F', linewidth=1.0, linestyle=':',
               label='1H SL')

    ts_str  = ts_sl_1h.strftime('%Y%m%d_%H%M')
    ax.set_title(
        f'USDJPY 5M [{direction}]  1H SL: {ts_sl_1h}  '
        f'(前{pre_bars}本 + SL足 + 後{post_bars}本)',
        color='white', fontsize=11
    )
    ax.legend(facecolor='#1e222d', labelcolor='white', fontsize=9)
    ax.tick_params(colors='#aaa')

    # 保存
    save_path = Path(save_dir)
    save_path.mkdir(parents=True, exist_ok=True)
    fname = save_path / f'{ts_str}_{direction}_1h_window.png'
    plt.savefig(fname, dpi=150, bbox_inches='tight',
                facecolor=fig.get_facecolor())
    plt.close()
    print(f"  saved: {fname}")
```

### plotter.py への呼び出し main 追加
```python
# plotter.py の if __name__ == '__main__' ブロックに追加

if __name__ == '__main__':
    import pandas as pd
    from pathlib import Path
    import sys
    sys.path.insert(0, str(Path(__file__).parent))

    from swing_detector import (
        get_direction_4h,
        get_nearest_swing_low,
        get_all_swing_lows_1h,
    )

    DATA_PATH = Path(__file__).parent.parent / 'data/raw/usdjpy_multi_tf_2years.parquet'
    df_5m = pd.read_parquet(DATA_PATH)

    def resample_tf(df, rule):
        return df.resample(rule, label='right', closed='right').agg(
            {'open':'first','high':'max','low':'min','close':'last'}
        ).dropna()

    df_4h = resample_tf(df_5m, '4h')
    df_1h = resample_tf(df_5m, '1h')

    # 一致イベントを再スキャン（test_1h_coincidence.py と同一ロジック）
    MATCH_THRESH = 10.0  # pips（10pips以内を一致とみなす）
    MAX_PLOTS    = 20    # 最大プロット件数

    plotted = 0
    WARMUP  = 50

    for i in range(WARMUP, len(df_4h)):
        if plotted >= MAX_PLOTS:
            break
        ts_4h = df_4h.index[i]
        direction = get_direction_4h(
            df_4h['high'].iloc[:i+1], df_4h['low'].iloc[:i+1],
            n=3, lookback=20
        )
        if direction != 'LONG':
            continue
        sl_4h = get_nearest_swing_low(
            df_4h['low'].iloc[:i+1], n=3, lookback=20
        )
        if sl_4h is None:
            continue
        idx_1h = df_1h.index.searchsorted(ts_4h, side='right') - 1
        if idx_1h < 240:
            continue
        all_sl_1h = get_all_swing_lows_1h(
            df_1h['low'].iloc[:idx_1h+1], n=2, lookback=240
        )
        if len(all_sl_1h) == 0:
            continue
        sl_1h_ts  = all_sl_1h.idxmin()
        sl_1h_val = float(all_sl_1h.min())
        dist = abs(sl_4h - sl_1h_val) / 0.01

        if dist <= MATCH_THRESH:
            plot_1h_window_5m(
                df_5m=df_5m,
                df_1h=df_1h,
                ts_sl_1h=sl_1h_ts,
                sl_4h=sl_4h,
                direction=direction,
            )
            plotted += 1

    print(f"\n合計 {plotted} 枚 → logs/1h_windows/")
```

---

## 完了条件

- [ ] get_nearest_swing_low_1h() / get_all_swing_lows_1h() が swing_detector.py に追加されている
- [ ] python src/test_1h_coincidence.py が実行できる
- [ ] logs/test_1h_coincidence.csv が生成されている
- [ ] python src/plotter.py が実行できる
- [ ] logs/1h_windows/ に PNG が最大 20 枚保存されている
- [ ] git commit -m "Feat: #020 1H-4H coincidence test + window plot"

## 完了後の報告フォーマット

以下をそのまま Rex に貼り付けること:

=== #020 結果報告 ===
対象サンプル (4H LONG) :
1H SL 検出率           :    %
4H SL との距離
  mean / median :
一致率
  <=5pips  :    %
  <=10pips :    %
  <=20pips :    %
PNG 生成枚数 :

Rex からの補足（ボス向け）
作業②の結果で「一致率 <=10pips が 70%以上」であれば設計前提が成立していると判断し、③のプロットで目視確認に進む。それ以下の場合は一致条件の定義を調整する必要がある。プロットを見てから次のステップ（15Mエントリーロジック精査）を決めよう。その前に今回のロジックが設計の確認にもなるから、今後のスレ引き継でも使える形で随時更新できるアーキテクチャーを作っておこうか？19:40アーキテクチャドキュメントを設計し、更新可能な形式で構築した。アーキテクチャドキュメントを設計し、更新可能な形式で構築した。いい判断だ、ボス。指示書ベースで断片化していた設計を一枚の「生きたドキュメント」にまとめる。設計確認用の更新可能なアーキテクチャドキュメントを構築した。設計確認用の更新可能なアーキテクチャドキュメントを構築した。トレーディングターミナル風に仕上げた。タブ構成は以下の7セクション。
Overview — KPI・ファイル構成・戦略の本質を一目で確認
Strategy — 3層アーキテクチャ図 / エントリー3段階フロー / 決済4段階 / 再エントリーロジック
Parameters — entry_logic.py の確定値・Swing検出パラメータ一覧・廃止済みパラメータ
Backtest — 結果推移テーブル / パターン別勝率バー / base_scanner / structure_plotter 知見
Instructions — #001〜#022の完了履歴と状態（完了・実装中・未着手）
Issues — 優先度別（赤 / 黄 / 未着手）で未解決課題を管理
Handoff — 新スレッド用の引き継ぎプロンプトをそのままコピーできる形で格納

更新ルール
指示書完了のたびに:
  1. Instructions タブ → 状態を「完了」に変更
  2. Issues タブ → 該当課題を削除 or 次課題を追加
  3. Handoff タブ → 引き継ぎ文の「最終指示書」番号と「次のアクション」を更新
  4. Backtest タブ → 新しい数値を追記

更新担当: Rex が設計後に更新版を出力
          → ボスがプロジェクトに上書き保存
プロジェクトファイルに REX_ARCHITECTURE.html として保存してくれれば、次スレッドから ultrathink フラグとともに最初に読み込む体制が整う。#020 の結果が出たら即反映する。Rex architectureコード · HTML Google Chromeで開くEX_DESIGN_CONFIRMED-2026-3-22.md365行mdあとEX_DESIGN_CONFIRMED-2026-3-22.mdに関しても現状のものに訂正して後で確認できるように更新しておいてくれるかな？19:52EX_DESIGN_CONFIRMED ファイルを現状に合わせて刷新中。EX_DESIGN_CONFIRMED ファイルを現状に合わせて刷新中。mdファイルなのでそのまま作成する。ファイルを作成しました, ファイルを読み取りましたファイルを作成しました, ファイルを読み取りました旧版（-3-22）との主な差分をまとめておく。
追加・変更した箇所:
セクション3に MTF 階層スキャン構造（#020設計確定） を丸ごと新設。3層の定義・パラメータ・ウィンドウ設計（前20/後5本）を明文化した。
Support_1h 定義 を「暫定/正式」の2段構成に書き換え。lookback=20（5時間）の矛盾と、lookback=240（10日）への変更根拠を明記。
再エントリー仕様（3-6） に「5M損切後 → 1H戻り高値未達 → 次の5M DBが1H 2番底になる」という構造的根拠を追加。
セクション5のSwing検出パラメータ表に 1H support_1h（正式） 行と 1H window 前後（#020） 行を追加。
セクション7の指示書履歴に #020（実装中）/ #021 / #022 を追記。
セクション10（旧）を 10（#019目視検証サマリー） と 11（#020設計方針） に分割して詳細化。合格ライン・ウィンドウ設計根拠・新規関数仕様を明記。
セクション16 に引き継ぎ用サマリーを追加（新スレッドの最初に ultrathink と一緒に貼るだけで済む形）。
REX_ARCHITECTURE.html と合わせてプロジェクトに両方保存しておけば、次スレッドの立ち上げが確実に速くなる。


PF (最新 #018)
5.32
★★★ LONG のみ
勝率
55.0%
20件
MaxDD
14.9p
過去最低
総損益
+91.6p
2年分バックテスト
プロジェクト
コード名	Project Invasion
対象	USDJPY 5M
リポジトリ	Minato33440/UCAR_DIALY
ローカル	C:\Python\REX_AI\Trade_System\
データ	usdjpy_multi_tf_2years.parquet
チーム
Minato	ディレクター・意思決定
Rex (claude.ai)	設計・分析・指示書作成
ClaudeCode (VS Code)	実装・Git 管理
戦略の本質
「4H 上昇ダウが継続している限り、押し目条件が揃うたびにエントリーを繰り返す構造」
エリオット波数カウントは実装しない。初動3波狙いは裁量表現であり、コード上は使わない。
完了ファイル構成
src/
├── swing_detector.py      ✅  detect_swing_highs/lows, get_direction_4h, get_nearest_swing_high_1h, get_nearest_swing_low_1h
├── entry_logic.py         ✅  check_fib_condition, check_15m_range_low, check_5m_double_bottom, evaluate_entry
├── exit_logic.py          ✅  check_5m_dow_break, check_15m_dow_break, manage_exit（4段階）
├── plotter.py             ✅  plot_swing_check, plot_4h_1h_structure, plot_1h_window_5m ← #020予定
├── backtest.py            ✅  ポジション管理 neck_1h / exit_phase / entry_pattern / grade
├── base_scanner.py        ✅  scan_4h_15m_base
├── structure_plotter.py   ✅  scan_4h_neck_breaks（N_4H=5, N_1H=3）
├── test_1h_coincidence.py 🔄  ← #020 作業中
└── volume_alert.py        ⬜ Phase D（未着手）


あとEX_DESIGN_CONFIRMED-2026-3-22.mdに関しても現状のものに訂正して後で確認できるように更新しておいてくれるかな？

3月24日

設計ファイルを現状に合わせて更新することを検討した。
設計ファイルを現状に合わせて更新することを検討した。
EX_DESIGN_CONFIRMED-2026-3-24.md として更新した。3-22版からの主な変更点を整理しておく。

3-22版からの更新内容
追加・変更したセクション:

セクション	変更内容
§3-2	MTF階層スキャン構造（3層）を新設 — #020設計確定
§3-5 再エントリー	1H戻り高値未達 → 次5M DB = 1H2番底の論理を明文化
§4 ファイル構成	get_nearest/all_swing_low_1h・test_1h_coincidence.py・plot_1h_window_5m・REX_ARCHITECTURE.html・logs/1h_windows/ を追加
§5 パラメータ	1H support_1h(lookback=240)を「#021変更予定」として追記、状態列を追加
§7 指示書履歴	#020〜#022を追加（状態列付き）
§10 目視検証結果	#019プロット9枚の検証結果を記録（合格9/10）
§11 #020設計詳細	新規追加関数・検証スクリプト合格ライン・ウィンドウ定義・プロット仕様を明記
§12 次ステップ	#020報告フォーマット・#021〜#022の条件を明文化
§15 プロット設計	plot_1h_window_5m の仕様を追記
§16 設計管理ファイル	REX_ARCHITECTURE.html の位置付けと引き継ぎ手順を新設
プロジェクトの docs/ に保存してくれ、ボス。#020の結果が出たら§12の報告フォーマット欄を埋めて §11と§12を再更新する。

Ex design confirmed 2026 3 24
ドキュメント · MD 

PLOT_DESIGN_CONFIRMED.md
230行

md

PLOT_DESIGN_CONFIRMED.mdの方も現行版で変更点があれば更新してくれるかな。

3月24日

ファイルを更新し、最新の変更を反映させた。
ファイルを更新し、最新の変更を反映させた。
PLOT_DESIGN_CONFIRMED-2026-3-24.md として更新した。3-20版からの主な変更点を整理する。

3-20版からの更新内容
セクション	変更内容
§2 プロット関数一覧	新設 — 4関数の状態・保存先を一覧化
§3 トリガー条件	関数ごとに分割。WICKTOL=5.0を反映、plot_4h_1h_structure / plot_1h_window_5m のトリガーを追加
§4 チャート構成	3関数それぞれのパネル仕様を明記
§5 色定義	使用関数列を追加。plot_1h_window_5m 用の色（5M SH/SL マーカー・1H SL 垂直線）を追記
§6 表示期間	関数ごとに分割。plot_4h_1h_structure（左10日/右2日）・plot_1h_window_5m（26本 = 26時間）を追加
§7 保存仕様	3関数分の保存先・解像度・上限を整理
§8 実装フェーズ	Phase 1「完了」に更新、Phase 1.5（#019）・Phase 2前段（#020）を追加。各フェーズの達成状況を記録
§10 統合方針	追加済み関数と呼び出し元の現状を反映
§11 完了条件	フェーズごとの完了チェックリストを整理（✅達成済み / □未達成）
docs/ フォルダに上書き保存してくれ、ボス。これで3ファイルが現行版に揃った。

Plot design confirmed 2026 3 24
ドキュメント · MD 
指示書 #020はまだClaudeCode実装してもらってないけど修正箇所がないか再チェックを。

3月24日

指示書#020の実装前に3つの重大なバグを発見し検証した。
指示書#020の実装前に3つの重大なバグを発見し検証した。
了解。指示書 #020 を精査する。


指示書#020の実装前に複数のバグを発見し修正案を検討した。
指示書#020の実装前に複数のバグを発見し修正案を検討した。
3箇所にバグを発見した。

バグ報告 — 指示書 #020 チェック結果
Bug 1（Critical）— make_addplot() に存在しない ax 引数
python
# 誤（ax= は make_addplot の引数として存在しない → TypeError）
mpf.make_addplot(sh_vals, type='scatter', marker='v',
                 markersize=60, color='#FA8072', ax=ax)

# 正
mpf.make_addplot(sh_vals, type='scatter', marker='v',
                 markersize=60, color='#FA8072')
ax の指定は mpf.plot() 側で returnfig=True を使って取得するのが正しいパターン。

Bug 2（Critical）— mplfinance の使用パターンが非標準
python
# 誤（plt.subplots で ax を作ってから mpf.plot に渡すパターン）
fig, ax = plt.subplots(figsize=(fig_w, fig_h))
mpf.plot(df_5m_win, type='candle', style=s,
         addplot=apds, ax=ax, returnfig=False)
# → mplfinance が ax を無視して別の figure に描画する場合がある

# 正（returnfig=True でfig/axesを受け取る）
fig, axes = mpf.plot(
    df_5m_win,
    type='candle',
    style=s,
    addplot=apds,
    returnfig=True,
    figsize=(fig_w, fig_h),
)
ax = axes[0]
fig.patch.set_facecolor('#131722')
ax.set_facecolor('#131722')
既存の plot_swing_check() も returnfig=True パターンを使っているはずなので、統一が必要。

Bug 3（Critical）— axvline() に integer を渡している
python
# 誤（get_loc → integer → mplfinance の datetime 軸とミスマッチ）
ax.axvline(x=df_5m_win.index.get_loc(
    df_5m_win.index[df_5m_win.index.searchsorted(ts_sl_1h)]
), ...)

# 正（Timestamp を直接渡す）
idx_in_win = df_5m_win.index.searchsorted(ts_sl_1h, side='left')
if idx_in_win < len(df_5m_win):
    ts_for_vline = df_5m_win.index[idx_in_win]
    ax.axvline(x=ts_for_vline, color='#ADFF2F',
               linewidth=1.0, linestyle=':', label='1H SL')
Minor — dist is not None チェックが冗長
python
# 検出済みパス（len(all_sl_1h) > 0）では dist は常に float
# → None チェックは不要だが害もないので放置可
'dist_pips': round(dist, 2) if dist is not None else None  # ← 冗長
'dist_pips': round(dist, 2)                                 # ← シンプル
```

---

## 修正版 #020 指示書（plot_1h_window_5m 作業③のみ差し替え）
```
# REX 指示書 #020 — 1H-4H 一致検証 + 窓プロット（修正版）
# 作成: Rex / 承認: Minato / 対象: ClaudeCode
# 作成日: 2026-03-24
# 前提: 指示書 #001〜#019 完了済み

think harder

---

## 作業概要（変更なし）

| # | 対象ファイル              | 内容                                    |
|---|---------------------------|-----------------------------------------|
| ① | swing_detector.py         | get_nearest_swing_low_1h() 追加         |
| ② | src/test_1h_coincidence.py | 1H SL ≒ 4H SL 一致検証スクリプト 新規  |
| ③ | plotter.py                | plot_1h_window_5m() 追加（修正版）      |

---

## 作業① / ② （前回指示書と同一・変更なし）

---

## 作業③ plotter.py — plot_1h_window_5m()（修正版）
```python
def plot_1h_window_5m(
    df_5m: pd.DataFrame,
    df_1h: pd.DataFrame,
    ts_sl_1h: pd.Timestamp,
    sl_4h: float,
    direction: str = 'LONG',
    pre_bars: int = 20,
    post_bars: int = 5,
    save_dir: str = 'logs/1h_windows',
) -> None:
    import mplfinance as mpf
    import matplotlib.pyplot as plt
    from pathlib import Path
    from swing_detector import detect_swing_highs, detect_swing_lows

    # ---- 1H ウィンドウ範囲を確定 ----
    idx_sl       = df_1h.index.get_loc(ts_sl_1h)
    win_start_1h = df_1h.index[max(0, idx_sl - pre_bars)]
    win_end_1h   = df_1h.index[min(len(df_1h) - 1, idx_sl + post_bars)]

    # ---- 5M 範囲を抽出 ----
    df_5m_win = df_5m.loc[win_start_1h:win_end_1h].copy()
    if len(df_5m_win) < 10:
        return

    # ---- 5M SH / SL 検出（窓内限定）----
    sh_mask = detect_swing_highs(df_5m_win['high'], n=2)
    sl_mask = detect_swing_lows(df_5m_win['low'],   n=2)
    sh_vals = df_5m_win['high'].where(sh_mask)
    sl_vals = df_5m_win['low'].where(sl_mask)

    # ---- mplfinance スタイル ----
    mc = mpf.make_marketcolors(
        up='#26a69a', down='#ef5350',
        edge='inherit', wick='inherit', volume='in'
    )
    s = mpf.make_mpf_style(
        marketcolors=mc, facecolor='#131722',
        edgecolor='#444', gridcolor='#2a2e39'
    )

    # ---- addplot（ax= は渡さない） ----
    apds = [
        mpf.make_addplot(sh_vals, type='scatter', marker='v',
                         markersize=60, color='#FA8072'),
        mpf.make_addplot(sl_vals, type='scatter', marker='^',
                         markersize=60, color='#87CEEB'),
    ]

    # ---- returnfig=True でfig/axesを取得（正しいパターン） ----
    fig, axes = mpf.plot(
        df_5m_win,
        type='candle',
        style=s,
        addplot=apds,
        returnfig=True,
        figsize=(16, 7),
    )
    ax = axes[0]
    fig.patch.set_facecolor('#131722')
    ax.set_facecolor('#131722')

    # 4H SL 水平線
    ax.axhline(y=sl_4h, color='#1E90FF', linewidth=1.5,
               linestyle='--', label=f'4H SL {sl_4h:.3f}')

    # 1H SL 足の垂直線（Timestamp を直接渡す）
    idx_in_win = df_5m_win.index.searchsorted(ts_sl_1h, side='left')
    if idx_in_win < len(df_5m_win):
        ts_for_vline = df_5m_win.index[idx_in_win]
        ax.axvline(x=ts_for_vline, color='#ADFF2F',
                   linewidth=1.0, linestyle=':',
                   label='1H SL')

    ts_str = ts_sl_1h.strftime('%Y%m%d_%H%M')
    ax.set_title(
        f'USDJPY 5M [{direction}]  1H SL: {ts_sl_1h}  '
        f'(前{pre_bars}本 + SL足 + 後{post_bars}本)',
        color='white', fontsize=11
    )
    ax.legend(facecolor='#1e222d', labelcolor='white', fontsize=9)
    ax.tick_params(colors='#aaa')

    # 保存
    save_path = Path(save_dir)
    save_path.mkdir(parents=True, exist_ok=True)
    fname = save_path / f'{ts_str}_{direction}_1h_window.png'
    plt.savefig(fname, dpi=150, bbox_inches='tight',
                facecolor=fig.get_facecolor())
    plt.close(fig)
    print(f"  saved: {fname}")
```

---

## 完了条件（修正なし）

- [ ] get_nearest_swing_low_1h() / get_all_swing_lows_1h() 追加済み
- [ ] python src/test_1h_coincidence.py が実行できる
- [ ] logs/test_1h_coincidence.csv が生成されている
- [ ] python src/plotter.py が実行できる（TypeError なし）
- [ ] logs/1h_windows/ に PNG が最大 20 枚保存されている
- [ ] git commit -m "Feat: #020 1H-4H coincidence test + window plot"
作業①②はバグなし。作業③の plot_1h_window_5m() だけ差し替えれば ClaudeCode に投げられる状態だ、ボス。